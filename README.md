# robolabel

Lightweight RoboInter-style annotation tool.

## Run from source

```bash
python -m lite_annotator.app
```

## Build executable

```bash
python -m PyInstaller robolabel.spec --clean --noconfirm
```

The executable is generated at:

```text
dist/robolabletools
```
